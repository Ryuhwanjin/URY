# URY v0.9.6

## Studio 강의노트 저장 안정화

- 날짜별 강의노트 생성 결과를 해당 주차 파일과 전체 통합 파일에 함께 적재합니다.
- 같은 날짜를 다시 생성하면 기존 섹션을 교체해 중복을 방지합니다.
- 1,000자 미만이거나 필수 4개 섹션이 없는 스트리밍 응답은 저장하지 않습니다.
- 잘린 영문 응답은 다음 Gemini 모델로 전환하며, 영문 실패 시 잘린 파일을 남기지 않습니다.
- macOS·Windows 공용 생성 코드를 동일하게 반영했습니다.

## 검증

- 자동 테스트 38개 통과, 1개 환경 의존 테스트 스킵
- Python 구문 검사 및 `git diff --check` 통과
- macOS 앱 ad-hoc 서명·GUI smoke test 후 DMG 생성

개인 API Key, 시간표, 강의자료, 생성 결과는 릴리즈 파일에 포함하지 않습니다.
